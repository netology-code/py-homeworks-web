# Практическое задание «Event loop. Asyncio»

## Информация для студентов

Уважаемые студенты! В этом задании вы примените на практике асинхронное программирование в Python. Вам предстоит загрузить данные о персонажах вселенной Star Wars из открытого API и сохранить их в базу данных PostgreSQL — полностью в асинхронном режиме, без блокирующих вызовов.

Почему это важно? В реальных проектах часто требуется массово загружать данные из внешних источников. Синхронный подход при этом крайне неэффективен — каждый HTTP-запрос блокирует выполнение программы. Асинхронный пайплайн позволяет выполнять десятки запросов параллельно, многократно ускоряя процесс.

## Выполнив это задание, вы:

- научитесь работать с `asyncio` и event loop в Python;
- освоите асинхронные HTTP-запросы с помощью `aiohttp`;
- научитесь асинхронно взаимодействовать с PostgreSQL через `asyncpg` (или `aiopg`);
- получите опыт создания миграций с помощью SQLAlchemy;
- научитесь разворачивать PostgreSQL в Docker-контейнере.

## Время на выполнение задания: 240 минут.

## Чек-лист готовности к практическому заданию:

- Изучите лекцию «Event loop. Asyncio».
- Убедитесь, что на вашем компьютере установлены Python 3.10+, Docker и Docker Compose.

## Инструменты, которые пригодятся для выполнения задания:

- Python 3.10+.
- Docker / Docker Compose.
- Библиотеки: `aiohttp`, `asyncpg` (или `aiopg`), `sqlalchemy`.

---

## Описание задания

### Источник данных

Вы будете работать с API **Star Wars API (SWAPI)**: `https://swapi-node.vercel.app/api/people/`

Список всех персонажей доступен постранично:
```
GET https://swapi-node.vercel.app/api/people?page=1&limit=10
```

Ответ содержит поля `count`, `pages`, `next`, `previous` и массив `results`, где каждый элемент — объект с ключом `fields`, содержащим все данные персонажа. Детальная информация о персонаже также доступна напрямую:
```
GET https://swapi-node.vercel.app/api/people/{id}
```

Данные персонажа находятся в `fields` (как в списке, так и в детальном запросе).


### Описание полей персонажа

| Поле | Тип в API | Описание | Тип в БД | Примечание |
|------|-----------|----------|----------|------------|
| `id` | `string` | Уникальный идентификатор персонажа | `String` | Извлекается из `fields.url` (например `/api/people/1` → `1`) |
| `birth_year` | `string` | Год рождения (например, `19BBY`) | `String` | — |
| `eye_color` | `string` | Цвет глаз | `String` | — |
| `films` | `array[path]` | Список путей фильмов, в которых появлялся персонаж | `String` | Перейти по каждому пути (добавив базовый URL), извлечь поле `title`, записать через запятую |
| `gender` | `string` | Пол персонажа | `String` | — |
| `hair_color` | `string` | Цвет волос | `String` | — |
| `height` | `string` | Рост в сантиметрах | `String` | — |
| `homeworld` | `path` | Относительный путь к родной планете | `String` | Перейти по пути (добавив базовый URL), извлечь поле `name` |
| `mass` | `string` | Масса в килограммах | `String` | — |
| `name` | `string` | Имя персонажа | `String` | — |
| `skin_color` | `string` | Цвет кожи | `String` | — |
| `starships` | `array[path]` | Список путей звёздных кораблей | `String` | Перейти по каждому пути (добавив базовый URL), извлечь поле `starship_class`, записать через запятую |
| `vehicles` | `array[path]` | Список путей транспортных средств | `String` | Перейти по каждому пути (добавив базовый URL), извлечь поле `name`, записать через запятую |


### Пример

Для персонажа Luke Skywalker (`https://swapi-node.vercel.app/api/people/1`):

- `homeworld`: API возвращает `/api/planets/1` → GET `https://swapi-node.vercel.app/api/planets/1` → получаем `name: "Tatooine"` → в БД записываем `"Tatooine"`
- `films`: API возвращает пустой массив `[]` → в БД записываем `""`
- `starships`: API возвращает `["/api/starships/12", "/api/starships/22"]` → GET каждого → получаем `starship_class` → в БД записываем `"Starfighter, Armed government transport"`
- `vehicles`: API возвращает `["/api/vehicles/14", "/api/vehicles/30"]` → GET каждого → получаем `name` → в БД записываем `"Snowspeeder, Imperial Speeder Bike"`

### Что нужно сделать

1. **Развернуть PostgreSQL в Docker.** Подготовьте `docker-compose.yml` с сервисом PostgreSQL.

2. **Написать миграционный скрипт** (`models.py` или `migrate.py`). С помощью SQLAlchemy опишите модель таблицы `people` со всеми полями из таблицы выше. Все поля — строковые (`String`). Скрипт должен создавать таблицу в базе данных.

3. **Написать скрипт загрузки данных** (`load_data.py`). Скрипт должен:
 - Асинхронно получить список всех персонажей из SWAPI (82 записи) через пагинацию — все данные персонажей уже содержатся в `results[i].fields`;
   - Для каждого персонажа асинхронно загрузить детальную информацию;
   - Для полей-путей (`homeworld`, `films`, `starships`, `vehicles`) — асинхронно выполнить GET-запрос на `https://swapi-node.vercel.app` + путь и извлечь нужное поле;
   - Асинхронно сохранить все записи в PostgreSQL;
   - Использовать **только асинхронные** библиотеки (`aiohttp` для HTTP, `asyncpg`/`aiopg` для БД).

4. **Убедиться**, что в базе данных сохранены все 82 персонажа с заполненными полями.

---

## Инструкция к выполнению

### Шаг 1. Подготовка окружения

Создайте файл `docker-compose.yml`:

```yaml
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: swapi_user
      POSTGRES_PASSWORD: swapi_pass
      POSTGRES_DB: swapi
    ports:
      - "5432:5432"
```

Запустите контейнер:
```bash
docker compose up -d
```

Установите зависимости:
```bash
pip install aiohttp asyncpg sqlalchemy
```

### Шаг 2. Миграция — создание таблицы

Создайте файл `models.py`. Опишите модель `SwapiPeople` с помощью SQLAlchemy (declarative base). Все поля — `String`. Первичный ключ — `id`.

Создайте файл `migrate.py`, который подключается к базе и создаёт таблицу.

### Шаг 3. Скрипт загрузки данных

Создайте файл `load_data.py`. Реализуйте:

1. Получение списка всех персонажей через пагинацию API (`?page=N&limit=10`) — все поля персонажа доступны в `results[i].fields`, отдельные запросы по `{id}` не нужны.
2. Параллельное разрешение путей для каждого персонажа:
   - `homeworld` → GET запрос на `https://swapi-node.vercel.app` + путь → извлечь `name`
   - `films` → GET запросы на `https://swapi-node.vercel.app` + каждый путь → извлечь `title` из каждого → объединить через `", "` (если массив пустой — записать `""`)
   - `starships` → GET запросы на `https://swapi-node.vercel.app` + каждый путь → извлечь `starship_class` из каждого → объединить через `", "`
   - `vehicles` → GET запросы на `https://swapi-node.vercel.app` + каждый путь → извлечь `name` из каждого → объединить через `", "`
3. Асинхронную вставку записей в PostgreSQL.

Все HTTP-запросы и операции с БД должны быть асинхронными. Используйте `asyncio.gather()` или `asyncio.TaskGroup` для параллельного выполнения запросов.

### Шаг 4. Проверка результата

Подключитесь к базе и убедитесь, что данные загружены:
```bash
docker exec -it <container_id> psql -U swapi_user -d swapi -c "SELECT count(*) FROM people;"
```

Ожидаемый результат: `82`.

```bash
docker exec -it <container_id> psql -U swapi_user -d swapi -c "SELECT name, homeworld, films FROM people LIMIT 3;"
```

---

## Перед сдачей задания убедитесь, что вы:

- Развернули PostgreSQL в Docker.
- Миграционный скрипт создаёт таблицу `people` со всеми необходимыми полями.
- Скрипт загрузки использует **только асинхронные** библиотеки.
- В базе данных сохранены все 82 персонажа.
- Поля-пути (`homeworld`, `starships`, `vehicles`) содержат имена/названия, а не пути; `films` может быть пустой строкой.
- Код запускается и работает без ошибок.

Вставьте ссылку на ваш репозиторий в поле «Ссылка на решение» и нажмите «Отправить на проверку». 

---


- Отсутствует миграционный скрипт или `docker-compose.yml`.
- Таблица содержит не все обязательные поля.

### «Незачёт»

Ставится в крайнем случае, если вы присылаете пустое или недоработанное задание во второй раз после отправки работы на доработку.
